

function stringLength(a, b, c) {

    let sum = a.length + b.length + c.length;
    let sumAverage = sum / 3;
    console.log(sum);
    console.log(Math.round(sumAverage));


}


stringLength('chocolate', 'ice cream', 'cake');
